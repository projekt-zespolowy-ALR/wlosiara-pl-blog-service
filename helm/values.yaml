port: 3000

postgresql:
  auth:
    database: "wlosiara-pl-blog-service"
    username: "wlosiara-pl-blog-service"
  primary:
    initdb:
      scripts-configmap: "wlosiara-pl-blog-service-postgresql-initdb-scripts-configmap"
    resources:
      requests:
        cpu: "50m"
        memory: "128Mi"

image:
  dockerRegistry:
    host: null # required

resources:
  requests:
    cpu: "50m"
    memory: "128Mi"
  limits:
    cpu: "100m"
    memory: "256Mi"
